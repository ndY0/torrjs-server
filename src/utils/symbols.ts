const keyForRouteMap = Symbol("key_for_route_map");
const keyForMetadataRouteMap = Symbol("key_for_metadata_route_map");
const keyForBasePath = Symbol("key_for_base_path");
const keyForServer = Symbol("key_for_server");
const keyForBaseMiddlewares = Symbol("key_for_base_middlewares");
const keyForServerPort = Symbol("key_for_server_port");

export {
  keyForRouteMap,
  keyForMetadataRouteMap,
  keyForBasePath,
  keyForServer,
  keyForBaseMiddlewares,
  keyForServerPort,
};
